{
  "name": "pr-dependency-graph",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for Vercel serverless functions in /api. The React app lives in /client.",
  "devDependencies": {
    "@vercel/node": "^5.7.13",
    "typescript": "^6.0.2"
  }
}
